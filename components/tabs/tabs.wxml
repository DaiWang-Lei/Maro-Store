<view class="tabs">
  <view class="tabs_title">
    <view class="tabs_item {{item.Active?'active':''}}" wx:for="{{tabs}}" wx:key="id" bindtap="handleTitle"
    data-index='{{index}}'>{{item.value}}</view>
  </view>
  <view class="tabs_content">
  <slot></slot></view>
</view>